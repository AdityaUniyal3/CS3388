#version 410 core
layout(location = 0) in vec3 position;
layout(location = 1) in vec3 normal;

out vec3 position_worldspace;

void main() {
    position_worldspace = position;
    gl_Position = vec4(position, 1.0); // world position (no MVP here)
}
